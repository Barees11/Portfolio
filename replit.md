# Sunil Kumar Portfolio Site

Static single-page HTML site (`index.html`) for global trade compliance portfolio.

## Setup
- Served via `python3 -m http.server 5000 --bind 0.0.0.0` workflow.
- Deployment: static target, public dir `.`.
- Custom domain: `www.sunilkumarb.in` (CNAME).
